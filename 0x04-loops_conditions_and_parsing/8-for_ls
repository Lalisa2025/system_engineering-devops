#!/usr/bin/env bash
# Lists non-hidden files, printing only the part after the first dash
for f in *; do
  [ -e "$f" ] || continue
  if [[ "$f" == *-* ]]; then
    echo "${f#*-}"
  else
    echo "$f"
  fi
done
